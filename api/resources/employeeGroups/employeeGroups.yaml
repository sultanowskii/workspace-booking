get:
  operationId: getEmployeeGroups
  summary: Get employee groups
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../schemas/employeeGroups/EmployeeGroup.yaml
post:
  operationId: createEmployeeGroup
  summary: Create employee group
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../schemas/employeeGroups/EmployeeGroup.yaml
  responses:
    "201":
      description: Success
      content:
        application/json:
          schema:
            $ref: ../../schemas/employeeGroups/EmployeeGroup.yaml
