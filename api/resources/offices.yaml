get:
  summary: Get offices
  operationId: getOffices
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../schemas/Office.yaml
